/**
 * The small print under a field on the auth screens — validation errors and the
 * password criteria. 14px SemiBold at every width.
 *
 * Uses the static `text-m-p3` rather than the responsive `text-p2`, which is
 * 16px on mobile. `FieldDescription` fills the same role app-wide but still
 * carries `text-p2`; changing it would move the unmeasured admin screens.
 */
export const fieldNote = 'text-m-p3 font-semibold';
